# =============================================================================
# MinnowVPN Docker Configuration
# =============================================================================
# Copy this file to .env and customize for your deployment.
# The setup.sh script will generate this automatically.
# =============================================================================

# Required: Your domain name (must have DNS pointing to this server)
DOMAIN=vpn.example.com

# Required: Email for Let's Encrypt certificate notifications
ACME_EMAIL=admin@example.com

# Required: Server's public IP address (for VPN endpoint)
VPN_PUBLIC_IP=1.2.3.4

# Optional: Custom ports (defaults shown)
HTTP_PORT=80
HTTPS_PORT=443

# Optional: Grafana subdomain (for monitoring stack)
GRAFANA_DOMAIN=grafana.example.com

# Optional: Watchtower auto-update schedule (cron format, default: 4am UTC daily)
WATCHTOWER_SCHEDULE=0 0 4 * * *

# Optional: Watchtower notification URL (Slack, Discord, etc.)
# See: https://containrrr.dev/shoutrrr/services/overview/
# WATCHTOWER_NOTIFICATION_URL=slack://token-a/token-b/token-c

# Optional: Timezone
TZ=UTC
